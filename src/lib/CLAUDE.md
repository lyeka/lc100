# lib/
> L2 | 父级: src/CLAUDE.md

## 成员清单
utils.js:    cn() 类名合并（clsx + tailwind-merge）
progress.js: 学习进度 store，useSyncExternalStore + localStorage（key: lc100-progress），导出 useProgress / useProgressStats hook，跨 tab 同步

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
