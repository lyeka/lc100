# AI / Agent 开发面试题集

## 一、LLM 原理与模型选型

1. Transformer 架构下，大模型从接收输入到逐 Token 生成输出的完整流程是什么？
   - 文本如何经过 Tokenization、Embedding 和位置编码转换为模型输入？
   - 自回归生成过程中，模型如何选择下一个 Token？

2. Self-Attention 是如何计算的？
   - 为什么需要 Query、Key、Value 三组向量？
   - 同一个 Token 在不同上下文中的表示为什么会不同？
   - Attention 的时间与空间复杂度有什么影响？

3. KV Cache 的作用和工作机制是什么？
   - KV Cache 如何降低自回归推理的计算量？
   - 长上下文和高并发场景下，KV Cache 会带来哪些显存压力？
   - DeepSeek-V3 的 MLA 与传统 Multi-Head Attention 在 KV Cache 优化上有什么差异？

4. 如何为一个 AI 应用选择基础模型？
   - 如何综合评估任务效果、上下文长度、工具调用、多模态能力、延迟、吞吐与成本？
   - 开源模型、闭源 API 和本地部署模型分别适合什么场景？
   - 如何验证一个模型是否真正支持视频、音频或图像等目标模态？
   - 如果不考虑成本约束，模型选型标准会发生什么变化？

5. 如何评估和比较不同模型在同一 Agent 系统中的表现？
   - 如何避免只凭主观体验评价模型？
   - 如何比较不同模型、不同 Thinking Level 或不同推理配置的效果与 ROI？
   - 模型升级后，如何确认新版本带来的真实收益和回归风险？

6. 为什么有些意图识别或分类任务更适合使用小模型，而不是直接调用大模型？
   - 如何构建训练集和评测集？
   - 如何使用大模型进行数据增强，同时控制生成数据的质量？
   - 单意图与多意图场景应分别采用哪些评测指标？

7. 如何通过 DPO、LoRA 等方法优化 Agent 中的模型？
   - 应该微调基础生成模型、意图模型还是 Reranker？
   - 如何通过消融实验判断微调带来的收益？

8. 将 Agent 从强模型切换到较弱模型时，Harness 需要做哪些适配？
   - 如何改善弱模型工具选择不准、参数生成错误和指令遵循不稳定的问题？
   - 哪些能力适合通过 Prompt、结构化约束、路由或确定性代码补偿？

9. 长视频理解系统应该如何选择输入和处理链路？
   - 系统应直接处理视频、分帧图像、音频，还是 ASR 转写文本？
   - 如何估算十几分钟到几十分钟视频的 Token 消耗？
   - 如何在信息完整度、上下文限制、延迟与成本之间权衡？

## 二、Agent 架构与运行时

1. 一个完整的 Agent 应该包含哪些核心模块？
   - 模型、指令、工具、规划、记忆、状态、护栏和可观测性之间如何协作？
   - 用户的一条消息从进入系统到返回结果，会经过哪些模块？

2. Agent 的核心运行循环是怎样的？
   - ReAct 循环中的思考、行动、观察和终止分别如何实现？
   - Agent 如何判断继续推理、调用工具、请求用户补充信息或结束任务？
   - 最大迭代轮数应该如何设置？每一轮应如何利用上一轮的执行结果？

3. 如何设计 Agent Runtime？
   - Runtime 应该如何管理模型调用、上下文、状态、工具执行和异常处理？
   - 决策节点与执行节点应该如何划分？工具调用应放在哪一层？
   - 同步、流式和异步任务分别应如何建模？

4. 什么场景应该使用 Agent，什么场景更适合规则、工作流或状态机？
   - 为什么某些诊断任务需要 Agent，而不是纯规则或固定状态机？
   - 如何避免在能够确定性解决的问题上滥用 Agent？
   - Agent、Workflow 和 Agentic Workflow 的边界是什么？

5. 在没有强状态机约束的情况下，如何保证长流程任务不偏移？
   - 如何维护目标、计划、完成条件和关键约束？
   - 如何处理任务中断、恢复、重试和幂等性？

6. 如何设计 Agent 的状态机与状态持久化？
   - 状态包含哪些字段，存储在哪里，如何更新？
   - 并发执行、失败重试和断点续跑时如何保证状态一致性？

7. 如何在直接调用模型 API、使用 Agent 框架和复用 Coding Agent Runtime 之间选型？
   - 自研 Runtime 与 LangGraph、CrewAI、Coze 等框架各有什么优缺点？
   - 为什么有些系统选择 Claude Code、Codex 等现成 Agent，而不是直接调用模型 API？
   - 框架过重可能造成哪些状态膨胀、存储压力或 OOM 问题？

8. Harness Engineering 是什么？
   - Harness 如何通过上下文、工具、权限、验证和执行环境约束模型的行为？
   - Harness 的设计如何影响不同模型的能力上限、稳定性和成本？
   - Harness 是否可能成为 AI 应用开发中的标准工程范式？

9. 如何设计面向用户交付物的 Agent 兜底机制？
   - 模型输出不完整、不合规或无法验证时如何处理？
   - 何时应该重试、降级、回退到确定性流程或引入人工确认？

10. 用户出现投诉、愤怒等异常情绪时，Agent 如何在不中断主流程的情况下进行识别和干预？

## 三、Prompt 与上下文工程

1. 如何构建可维护的 Prompt 模板？
   - System Prompt、用户问题、历史消息、工具描述和召回知识应如何组织？
   - 如何控制指令优先级并处理相互冲突的上下文？
   - 每次模型调用是否都需要携带 System Prompt？如何利用 Prompt Cache 降低开销？

2. 什么是 Agent 的上下文采集？
   - 提前注入上下文与让模型主动检索上下文各有什么优缺点？
   - 如何决定哪些信息常驻上下文，哪些信息按需加载？
   - 上下文采集可以通过哪些检索、索引或工具机制实现？

3. 用户对话零碎、跨轮次且意图跳跃时，如何判断当前任务和话题边界？
   - 同一 Session 中多个相近话题如何隔离，避免上下文串扰？
   - 什么时候应延续原任务，什么时候应创建新任务或新 Session？
   - 信息不足时，查询改写模块如何触发用户澄清？

4. 长程任务即将超过上下文窗口时，应如何压缩上下文？
   - 摘要、滑动窗口、结构化状态、检索式回放和子 Agent 独立上下文分别适合什么场景？
   - 如果不调用 LLM，如何裁剪工具输出、日志和错误信息？
   - 如何避免压缩后丢失需求细节、关键决策和未完成事项？

5. 上下文摘要应该在什么时机生成和更新？
   - 只在会话结束时生成摘要会产生什么问题？
   - 反复摘要如何避免事实漂移和信息逐步丢失？

6. 为什么 Todo List、计划文件或任务账本能够让 Coding Agent 更聚焦？
   - 这些外部状态应如何生成、更新和校验？
   - 如何防止计划本身过期或与实际代码状态不一致？

7. 如何在多天或多轮的长程开发任务中保持目标一致？
   - 如何持久化需求、架构决策、验收标准和未完成工作？
   - 任务恢复时，如何用最少上下文重建有效工作状态？

8. Prompt Cache 应该如何设计和评估？
   - 哪些上下文适合作为稳定前缀缓存？
   - 上下文频繁变化时，如何提高缓存命中率并控制失效风险？

## 四、状态、会话与记忆系统

1. 为什么 Agent 需要区分短期记忆和长期记忆？
   - 两类记忆分别应该存什么？
   - 记忆在什么时机写入、读取、更新和淘汰？
   - 短期记忆为什么更适合滑动窗口，而不是可能打乱语义顺序的 LRU？

2. 如何设计长期记忆的数据模型和存储架构？
   - 情景记忆、语义记忆、用户偏好和任务知识应如何分类？
   - 为什么使用向量检索，哪些场景还需要关键词或结构化查询？
   - 本地文件、关系数据库和向量数据库分别适合存储哪些记忆？

3. 如何识别、抽取和按需加载用户偏好？
   - 哪些信息值得进入长期记忆？
   - 偏好应该写入 System Prompt、全文加载，还是按当前问题检索？

4. 多轮对话中的记忆发生冲突时，应该如何治理？
   - 如何定义记忆的权威性、时效性和置信度？
   - 低权威记忆如何晋级为长期事实或架构决策？
   - 重复记忆如何合并，错误或过期记忆如何修正和遗忘？

5. 如何实现用户、租户、项目和 Session 之间的记忆隔离？
   - 如何防止一个用户访问另一个用户的长期记忆？
   - 如何避免其他项目或历史任务的记忆污染当前任务？

6. 记忆、RAG、上下文和 Shared State 有什么区别？
   - 哪些信息应该进入知识库，哪些应该成为记忆或运行时状态？
   - 已经有知识库时，为什么不能简单地把所有知识都放入记忆检索？

7. 多层记忆架构应该如何设计？
   - 每一层解决什么问题，为什么不能只使用一个向量库？
   - 如何评估增加记忆层级带来的收益与复杂度？

8. 如何比较不同 Agent 或 Coding Agent 产品的记忆机制？
   - 应从存储介质、提取策略、检索方式、隔离机制和生命周期哪些维度比较？
   - 如何避免只模仿产品表象，而忽略其运行时约束？

9. 本地 Agent 与云端 Agent 的记忆方案应如何取舍？
   - 本地存储在隐私和用户控制方面有什么优势？
   - 云端多租户场景如何处理数据隔离、安全与同步？

## 五、RAG、文档解析与检索

1. RAG 主要解决什么问题？
   - 超长上下文模型出现后，RAG 的必要性是否会下降？
   - 哪些场景适合直接使用长上下文，哪些场景仍需要检索？

2. PDF、Word、Excel、Markdown 等技术文档应如何解析和标准化？
   - Markdown 中引用的图片应如何存储、关联和检索？
   - 表格、版面、标题层级和图片说明如何保留？
   - 文本解析、OCR 与多模态模型之间如何选型？

3. OCR 出现错字、噪声、漏字或内容截断时，如何检测和纠正？
   - 什么情况使用确定性脚本，什么情况交给 LLM 或多模态模型？
   - OCR 文本及其位置坐标如何帮助恢复文档结构？

4. 多模态检索中，图像向量与文本向量不在同一表示空间时，如何实现对齐？

5. 为什么长文档需要切分 Chunk 后再向量化？
   - 不切分会对表示质量、召回精度和成本造成什么影响？
   - Chunk 重叠区域解决什么问题，重叠比例如何确定？

6. 常见的文档分块策略有哪些，应该如何选型？
   - 固定长度、语义分块、递归分块、标题层级分块和父子文档分块各有什么优缺点？
   - 父子文档混合分块解决什么问题？
   - 多级标题如何拼接到子块中，并控制父级内容是否参与召回？

7. 如何为 RAG 系统选择 Embedding、Reranker 和推理模型？
   - 模型的语言、领域、维度、上下文长度、延迟和部署成本如何影响选型？
   - 如何通过离线实验验证模型组合，而不是只依赖公开榜单？

8. 稀疏检索与稠密向量检索有什么区别？
   - BM25、关键词搜索和向量搜索分别适合什么场景？
   - 如何设计关键词召回与向量召回的融合排序？
   - 在代码检索中，grep 与 Embedding/RAG 各有什么优缺点？

9. 如何设计完整的 RAG 召回流程？
   - 查询理解、查询改写、多路召回、融合、Rerank 和上下文组装如何衔接？
   - 多维度查询改写如何实现，为什么需要并行化意图识别？

10. RAG 的检索效果应该如何评估？
    - Recall@K、Hit@K 等指标如何定义和计算？
    - Top-K 过大或过小分别会造成什么问题？
    - 如何构建覆盖真实查询、困难样本和冲突知识的评测集？

11. 余弦相似度和欧氏距离在高维向量空间中有什么差异？实际项目中应如何选择？

12. 为什么需要 Reranker？
    - Reranker 解决了向量召回中的哪些问题？
    - Rerank 后的截断 K 值如何确定？
    - 如何评估微调 Reranker 带来的实际收益？

13. Milvus、Chroma 等向量数据库分别适合什么场景？
    - 如何从数据规模、索引能力、过滤、扩展性和运维成本进行选型？

14. 文档发生局部更新时，如何进行增量索引而不是全量重建？
    - 如何识别新增、修改和删除的 Chunk？
    - 如何保证新旧索引切换期间的一致性？

15. 多个召回文档内容互相冲突时，RAG 应如何处理？
    - 如何结合时间戳、来源权威性和置信度排序？
    - 是否应该向用户展示冲突并推动知识维护者修正？

16. RAG 没有召回到相关知识时，如何约束模型避免编造？
    - 如何识别“无答案”状态？
    - 何时应该降级、澄清、扩大检索或明确拒答？

17. HyDE 为什么能够改善模糊 Query 的召回效果？
    - 它在哪些场景可能引入偏差或放大幻觉？

18. 如何系统优化一个 RAG 系统？
    - 如何分别定位解析、分块、召回、排序和生成阶段的问题？
    - 如何通过消融实验区分索引更新、Prompt、Reranker 和生成模型带来的收益？

## 六、Tool、Skill、MCP 与 A2A

1. Agent 工具应该如何设计？
   - 工具名称、描述、输入 Schema、输出结构和错误码应遵循哪些原则？
   - 如何保证工具调用的可验证性、幂等性和可恢复性？
   - 如何避免给模型暴露过多相似或低质量工具？

2. 如何控制模型的工具调用？
   - 如何校验工具选择和参数？
   - 工具调用失败、超时或返回异常结果时，Runtime 应如何处理？
   - 如何通过结构化约束和执行前检查提升弱模型的工具调用准确率？

3. Tool、Function Calling、Skill、Workflow 和 Subagent 分别适合什么场景？
   - Skill 相比 Function Calling 的优势和代价是什么？
   - 一个能力何时应该封装成 Tool，何时应该沉淀成 Skill 或 Workflow？
   - 如何避免 Skill 与 Tool 的职责重复？

4. Skill 的加载和执行原理是什么？
   - Agent 如何发现、选择、读取并遵循 Skill？
   - Skill 如何携带流程知识、脚本、模板和约束？
   - 如何编写、版本化、评测和迭代 Skill？

5. MCP 解决了什么问题？
   - MCP 与普通 Function Calling 的关系是什么？
   - MCP 的 STDIO、旧版 SSE 和 Streamable HTTP 等传输方式有什么区别？
   - MCP 服务的能力发现、连接管理、错误处理和授权应如何设计？

6. A2A 解决了什么问题？它与 MCP 有什么区别？
   - Agent 之间如何发现能力、委派任务和交换状态？
   - 为什么 Agent 间通信通常不应直接复用工具调用协议？
   - 什么情况下应该采用标准 A2A 协议，什么情况下可以自定义内部协议？

7. 如何设计 Agent 能力注册表或能力市场？
   - 能力应如何描述、发现、授权、版本化和下线？
   - 如何保证第三方能力的质量与安全？

8. Agent 如何安全地对接外部知识库和业务系统？
   - 外部能力的认证、凭证、权限、限流和失败隔离如何处理？
   - 如何避免外部系统异常拖垮 Agent 主流程？

## 七、Multi-Agent 协作

1. 什么情况下应该使用 Multi-Agent，而不是单 Agent？
   - Multi-Agent 能解决哪些单 Agent 难以解决的问题？
   - 额外的上下文、通信、延迟和 Token 成本是否值得？

2. Multi-Agent 常见的架构模式有哪些？
   - 主从、Supervisor、Agent Team、共享状态和分片状态模式有什么区别？
   - 如何选择集中式编排还是去中心化协作？

3. 用户任务进入 Multi-Agent 系统后，第一个 Agent 应如何选择？
   - 路由应基于规则、分类模型、LLM 还是能力匹配？
   - Agent 在执行过程中动态引入新 Agent 的决策依据是什么？

4. Agent 之间应该如何通信和交接任务？
   - 任务、上下文、产物、进度和错误应采用什么数据结构传递？
   - Handoff、Agent-as-Tool 和对等通信各适合什么场景？
   - 子任务结果应以流式还是非流式方式返回？

5. 如何防止多个 Agent 自由交流时陷入循环、互相推诿或无限对话？
   - 如何设计轮数、预算、终止条件和全局仲裁？
   - 如何识别没有新增信息的无效协作？

6. 主从模式下，如何处理子 Agent 的幻觉或错误结果？
   - 主 Agent 应如何验证、交叉检查和拒绝子 Agent 产物？
   - 哪些子任务结果必须经过确定性验证或人工确认？

7. 多个 Agent 的输出风格、代码意见或审查结论冲突时，如何收敛？
   - 如何定义统一标准、优先级和最终决策者？
   - 如何保留有价值的少数意见，而不是简单多数投票？

8. 单 Agent 与多 Agent 的上下文管理有什么区别？
   - 每个 Agent 应只看到自己的上下文，还是共享完整会话？
   - Shared State 应包含哪些信息，如何控制读写权限？

9. 长时间群组协作中，如何让长时间未发言的 Agent 重新获得必要上下文？
   - 是否需要为每个 Agent 维护个性化摘要或事件流？
   - 如何避免把全部聊天记录重复注入每个 Agent？

10. Multi-Agent 之间的工具应该如何继承和隔离？
    - 子 Agent 是否可以继续调用 Shell、网络或其他高风险工具？
    - 如何防止授权在 Agent、Subagent 和 Tool 的嵌套链路中被意外放大？

11. Multi-Agent 协作失败时如何恢复？
    - Agent 超时、离线、重复执行或产生部分结果时如何处理？
    - 如何保证任务状态、产物和消息的最终一致性？

## 八、Coding Agent 与 AI 开发流程

1. Claude Code 等 Coding Agent 执行一个开发任务的核心流程是什么？
   - 它如何理解仓库、制定计划、搜索代码、编辑文件、运行命令并验证结果？
   - Agent loop、工具系统、上下文管理和权限控制如何协同？

2. 如何从零设计一个简化版 Coding Agent？
   - 最小可用版本需要哪些工具、状态和终止条件？
   - 如何防止它只生成代码却不验证结果？

3. 在编程场景中，如何关联跨多轮提出的修改要求、函数信息和当前任务？
   - 如何维护需求变化、代码位置、已完成修改和待验证事项？
   - 用户突然切换目标时，如何判断是修改当前任务还是创建新任务？

4. Coding Agent 如何处理超大代码库和长上下文？
   - 如何提前分析并保存项目结构？
   - 如何只加载与当前问题相关的代码、符号和历史决策？
   - 关键词搜索、AST、LSP 和向量检索应如何组合？

5. 如何设计面向单元测试生成的 Agent？
   - 生成前如何分析代码有效性、依赖关系和可测试性？
   - 哪些代码难以通过 AST 或 LSP 自动生成高质量测试，应该如何过滤？
   - Mock 应如何生成和验证？

6. 如何统计生成测试的分支覆盖率？
   - 代码插桩的基本原理是什么？
   - 为什么高覆盖率不一定代表测试有效？
   - 如何同时评估断言质量、缺陷发现能力和稳定性？

7. 不同 AI 编程工具之间如何共享项目规则、上下文和长期记忆？
   - 哪些信息应该放入仓库规范、Skill、项目记忆或外部知识库？
   - 如何避免不同工具维护出互相冲突的规则副本？

8. AI 辅助开发过程中发生需求变更时应如何处理？
   - 规格说明、计划、测试和代码如何同步更新？
   - 如何识别旧需求留下的无效实现和过期上下文？

9. 如何保证 Coding Agent 加入新功能后，旧功能不发生行为漂移？
   - 如何建立回归测试、验收标准和变更影响分析？
   - Harness 或模型变更后如何进行基准回归？

10. 多人使用 Coding Agent 协同开发时，如何减少代码冲突？
    - 如何划分任务、文件所有权和 Agent 工作空间？
    - Git Worktree、独立分支和合并队列如何配合？

11. Coding Agent 依赖其他服务时，如何进行本地开发和测试？
    - 如何使用 Mock、契约测试、临时环境或沙箱模拟依赖？
    - 如何验证 Agent 生成代码与真实服务的兼容性？

12. 如何防止 Coding Agent 在持续数天的开发过程中偏离原始目标？
    - 如何保存愿景、需求、架构约束与验收标准？
    - 如何让 Agent 在每轮工作前检查当前实现与目标的差距？

13. 如何设计人类与 Coding Agent 的协作流程？
    - 哪些环节适合 Agent 自主完成，哪些环节必须由人决策或审查？
    - 为什么核心代码可能不适合未经审查地由 AI 直接生成？

14. 如何收集 Coding Agent 用户在实际使用中的问题并推动产品迭代？
    - 用户反馈、失败轨迹和回归用例如何关联？
    - 如何判断问题来自模型、Harness、工具、上下文还是产品流程？

## 九、评测、可观测性与自进化

1. 如何搭建完整的 Agent 评测体系？
   - 应该评测最终答案、任务完成率、步骤质量、工具调用、延迟、成本还是安全性？
   - 离线评测、在线评测和人工评审分别解决什么问题？

2. Agent 上线后如何自动收集和管理评测 Case？
   - 如何从用户反馈、失败请求、低置信度结果和异常轨迹中发现 Case？
   - 线上 Case 与线下回归集如何流转、去重、分级和版本化？

3. 如何用评测结果牵引 Agent 下一版本迭代？
   - 如何定位问题属于模型、Prompt、上下文、工具、记忆还是编排？
   - 修复一个 Case 后，如何验证没有引入其他能力回归？

4. Agent 的 Trace 应该记录哪些信息？
   - 如何追踪模型调用、工具调用、Handoff、记忆读写、A2A 通信和错误恢复？
   - 如何从 Trace 中识别无效步骤、循环、上下文浪费和权限风险？

5. 修改 Harness 后应该如何设计 Benchmark？
   - 如何建立基线并比较修改前后的成功率、步骤数、Token、延迟和成本？
   - 如何评估从十步缩短到五步是否真的提升了效果，而不是遗漏必要步骤？

6. 如何评测 Agent 在虚拟环境和真实工具环境中的表现？
   - 如何保证环境可复现、结果可验证且不会污染外部系统？
   - 仿真环境与真实环境之间的差距如何控制？

7. Agent 的自演进或自进化应该如何设计？
   - 可以自动优化 Prompt、Skill、路由、工具说明还是记忆策略？
   - 每次自动修改如何经过验证、审批、灰度和回滚？

8. 开放世界任务缺少明确 Ground Truth 时，如何评估自进化效果？
   - 如何设计 Proxy Reward？
   - 如何避免代理指标被钻空子或与真实用户价值背离？
   - 如何避免把 Benchmark 本身变成唯一优化目标而触发 Goodhart's Law？

9. 如何评测 Multi-Agent、记忆和 A2A 等单独模块？
   - 如何设计模块级指标和端到端指标？
   - 如何通过消融实验确认复杂架构确实优于简单方案？

10. Agent 评测为什么需要持续趋势而不是一次性分数？
    - 如何解释每日曲线的波动？
    - 数据分布、模型版本和外部工具变化时如何保持指标可比？

## 十、安全、权限、沙箱与护栏

1. Agent 的高风险动作应该如何控制？
   - 哪些动作必须执行前确认，哪些可以自动执行？
   - 如何实现风险分级、最小权限、参数校验、审批和审计？

2. Tool Guardrail 应该放在调用链的哪些位置？
   - 输入检查、权限检查、执行前确认、结果检查分别解决什么问题？
   - 如何保证重试或恢复执行时仍会重新校验关键条件？

3. Coding Agent 的安全机制应该如何设计？
   - 文件系统、Shell、网络、凭证和外部服务权限如何隔离？
   - 如何限制命令作用范围并避免破坏性操作？
   - 人工审批如何与长程任务和断点恢复结合？

4. 如何设计 Agent 的凭证管理？
   - API Key、OAuth Token 和用户授权如何安全存储、注入、轮换和撤销？
   - 如何避免凭证进入 Prompt、日志、Trace 或模型输出？

5. Multi-Agent 系统如何防止权限扩散？
   - Agent、Subagent 和 Tool 的权限应如何继承或收缩？
   - 如何避免低权限 Agent 借助高权限 Agent 间接完成未授权操作？

6. Agent 沙箱应该如何设计？
   - 计算、内存、磁盘、网络、进程和执行时间如何限制？
   - 沙箱应有状态还是无状态，任务结束后如何清理？
   - 如何在隔离强度、启动速度和运行成本之间权衡？

7. 什么是事故驱动的护栏建设？
   - 如何把线上事故和 Bad Case 转化为可执行的规则、评测与回归用例？
   - 如何避免护栏不断叠加后阻碍正常能力？

8. 如何通过轻量级 Gate 减少不必要的高风险执行和 Token 消耗？
   - 哪些参数、权限和前置条件适合用确定性代码直接拒绝？
   - Gate 与模型判断冲突时应如何处理？

## 十一、性能、成本、稳定性与云端架构

1. Agent 系统流量增长一百倍时，架构应该如何扩展？
   - 模型调用、检索、工具执行、状态存储和异步任务分别可能出现哪些瓶颈？
   - 哪些组件可以无状态横向扩展，哪些需要一致性设计？

2. 大模型高并发调用时，如何进行限流、排队、降级和成本控制？
   - 如何设置租户、用户、模型和工具维度的配额？
   - 模型过载时如何切换备用模型、缩短上下文或回退到非 Agent 流程？

3. 如何优化 Agent 的端到端延迟？
   - 模型推理、串行工具调用、检索和长链路编排分别如何优化？
   - 哪些步骤可以并行，哪些步骤必须串行？

4. 图片、视频或长任务生成耗时较长时，如何改善用户等待体验？
   - 流式输出、阶段进度、异步任务、通知和可取消执行如何设计？
   - 如何确保用户离开后仍能恢复任务和获取结果？

5. 如何控制长程 Agent 的 Token 成本？
   - 上下文压缩、Prompt Cache、Skill、结果裁剪和步骤优化分别能解决什么问题？
   - 如何在减少 Token 与保持成功率之间建立可量化的权衡？

6. 如何保证 Agent 对接外部系统后的稳定性？
   - 超时、重试、熔断、隔离、限流和幂等如何设计？
   - 如何处理外部接口部分成功、重复回调和长时间不可用？

7. 如何将本地 Agent 演进为可持续运行的云端多租户服务？
   - 本地执行和远端执行应如何划分？
   - 用户代码、记忆、凭证和任务状态如何隔离？
   - 云端常驻任务如何调度、暂停、恢复和回收？

8. 企业级 Agent 的运行环境应该如何管理？
   - 每个用户独享沙箱的成本问题如何解决？
   - 如何设计轻量化、按需启动、可复用且安全的沙箱？
   - 内存、磁盘和生成文件的生命周期如何管理？

9. 如何诊断 Agent 框架导致的状态或存储膨胀问题？
   - 如何定位 Checkpoint、消息历史、工具结果和 Trace 中的异常增长？
   - 如何在不破坏断点恢复能力的前提下进行压缩和清理？

10. 如何衡量一个 Agent 系统的业务规模和工程质量？
    - 并发量、任务成功率、P95 延迟、Token 成本、人工接管率和用户留存如何综合分析？
    - 如何避免只展示单一的高准确率或性能优化数字？

## 十二、项目架构与技术决策

1. 如何完整介绍一个 AI / Agent 项目？
   - 项目背景、目标用户、核心问题、整体架构和关键链路分别是什么？
   - 为什么这个问题值得使用 AI 或 Agent 解决？

2. 项目的技术方案为什么这样选？
   - 模型、框架、检索、记忆、工具和部署方式的选型依据是什么？
   - 选择当前方案时放弃了哪些替代方案，主要权衡是什么？

3. 项目最有价值的架构设计是什么？
   - 它解决了什么具体问题？
   - 有哪些数据、评测或线上结果能够证明其价值？

4. 项目中最大的技术难点和失败案例是什么？
   - 最初方案为什么失败？
   - 如何定位根因、修正方案并防止问题再次发生？

5. 如何讲清楚一个具体 Agent 业务流程？
   - 用户输入如何经过路由、上下文构建、规划、工具调用、验证和结果返回？
   - 各阶段的异常和兜底路径是什么？

6. 一个通用 Agent、行业 Agent 和 Coding Agent 应如何定位？
   - 产品边界、能力深度、工具体系和评测标准有什么区别？
   - 为什么选择通用能力或垂直场景？

7. Agent 项目从原型迭代到当前架构的过程是什么？
   - 哪些用户反馈、线上问题或评测结果推动了关键改版？
   - 如何避免为了追逐新概念而引入没有收益的复杂度？

8. 面向企业级场景，现有 Agent 还需要哪些改进？
   - 安全、权限、审计、租户隔离、稳定性、部署和运维方面有哪些差距？
   - 如何制定从个人工具到企业产品的演进路线？

9. 如何评价 Agent 项目的真实价值？
   - 有多少用户在什么场景下持续使用？
   - 项目带来了哪些效率、质量、成本或业务收益？
   - 如何区分演示效果与可重复的生产价值？

10. 如何比较一个 Agent 产品与同类产品？
    - 应从目标用户、运行时、上下文、记忆、工具、协作、安全和成本哪些维度比较？
    - 如何证明差异来自有效的产品与工程决策，而不是功能堆砌？

11. 未来人类与 Agent 的协作模式可能如何演进？
    - Agent 应该是助手、执行者、团队成员还是工作操作系统？
    - 不同自治程度下，人类的决策、监督和责任边界如何变化？

12. Agent 系统未来最值得演进的方向是什么？
    - 应优先提升模型能力、Harness、记忆、工具生态、多 Agent 协作还是评测体系？
    - 如何用真实问题和可验证收益决定演进优先级？

## 已归纳来源

- `origin/20260324字节番茄AI agent开发二面.md`
- `origin/5.7 wxg社招面委会面经 agent方向.md`
- `origin/7-27 传音agent开发一面.md`
- `origin/Agent开发 - 二面.md`
- `origin/字节 agent开发 1-3面面经 5月.md`
- `origin/字节AI应用开发一面｜刺激到手脚发麻.md`
- `origin/字节跳动Agent开发一面.md`
- `origin/小厂agent二面问题.md`
- `origin/小绿书agent一面（已挂）.md`
- `origin/深信服 社招 agent 全过程 agent 5月 开发.md`
- `origin/腾讯元宝社招 agent架构全流程面经.md`
- `origin/阿里国际 社招 agent开发-架构 1-3面5月.md`
- `origin/顺丰科技 AI应用开发工程师 一面面经.md`
- `origin/高德 5月 agent 社招面经.md`
