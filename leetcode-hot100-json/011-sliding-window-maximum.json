{
  "id": 11,
  "leetcodeId": 239,
  "title": "滑动窗口最大值",
  "category": "子串/单调队列",
  "difficulty": "Hard",
  "description": "给定数组和滑动窗口大小 k，返回窗口从左到右滑动时每个位置的最大值。",
  "examples": [
    {
      "input": "nums = [1,3,-1,-3,5,3,6,7], k = 3",
      "output": "[3,3,5,5,6,7]"
    }
  ],
  "hint": {
    "core": "单调递减双端队列，队首始终是当前窗口最大值的索引",
    "keyPoints": [
      "维护一个双端队列（存索引），保证队列中元素对应的值单调递减",
      "新元素入队前，从队尾弹出所有比它小的元素（它们不可能再成为最大值）",
      "窗口滑动时检查队首是否过期（索引超出窗口范围），过期则弹出"
    ]
  },
  "solutions": [
    {
      "name": "单调队列",
      "code": "func maxSlidingWindow(nums []int, k int) []int {\n    var deque []int // 存索引，对应值单调递减\n    res := make([]int, 0, len(nums)-k+1)\n    for i, v := range nums {\n        // 移除队尾所有比当前值小的\n        for len(deque) > 0 && nums[deque[len(deque)-1]] <= v {\n            deque = deque[:len(deque)-1]\n        }\n        deque = append(deque, i)\n        // 队首过期\n        if deque[0] <= i-k {\n            deque = deque[1:]\n        }\n        // 窗口形成后开始收集结果\n        if i >= k-1 {\n            res = append(res, nums[deque[0]])\n        }\n    }\n    return res\n}",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(k)"
    }
  ]
}