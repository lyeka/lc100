{
  "id": 56,
  "leetcodeId": 78,
  "title": "子集",
  "category": "回溯",
  "difficulty": "Medium",
  "description": "给定不含重复元素的整数数组，返回该数组所有可能的子集。",
  "hint": {
    "core": "回溯枚举子集：每个元素选或不选，用 start 索引避免重复",
    "keyPoints": [
      "每次递归都是一个合法子集，直接收集",
      "从 start 开始遍历，选择当前元素→递归→撤销",
      "start 保证只向后选，避免重复子集"
    ]
  },
  "solutions": [
    {
      "name": "回溯",
      "code": "func subsets(nums []int) [][]int {\n    var res [][]int\n    var path []int\n    var backtrack func(int)\n    backtrack = func(start int) {\n        res = append(res, append([]int{}, path...))\n        for i := start; i < len(nums); i++ {\n            path = append(path, nums[i])\n            backtrack(i + 1)\n            path = path[:len(path)-1]\n        }\n    }\n    backtrack(0)\n    return res\n}",
      "timeComplexity": "O(n*2^n)",
      "spaceComplexity": "O(n)"
    },
    {
      "name": "位运算枚举",
      "code": "func subsets(nums []int) [][]int {\n    n := len(nums)\n    total := 1 << n\n    res := make([][]int, 0, total)\n    for mask := 0; mask < total; mask++ {\n        var sub []int\n        for i := 0; i < n; i++ {\n            if mask>>i&1 == 1 {\n                sub = append(sub, nums[i])\n            }\n        }\n        res = append(res, sub)\n    }\n    return res\n}",
      "timeComplexity": "O(n*2^n)",
      "spaceComplexity": "O(n)"
    }
  ]
}
