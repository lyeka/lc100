{
  "id": 33,
  "leetcodeId": 148,
  "title": "排序链表",
  "category": "链表",
  "difficulty": "Medium",
  "description": "对链表进行排序。要求 O(n log n) 时间、O(1) 空间。",
  "hint": {
    "core": "归并排序——快慢指针找中点、递归拆分、合并有序链表",
    "keyPoints": [
      "快慢指针找中点，断开为两条子链",
      "递归排序左右两半",
      "合并两个有序链表（复用 merge two sorted lists）"
    ]
  },
  "solutions": [
    {
      "name": "归并排序",
      "code": "func sortList(head *ListNode) *ListNode {\n    if head == nil || head.Next == nil {\n        return head\n    }\n\n    // 快慢指针找中点，slow 停在前半段末尾\n    slow, fast := head, head.Next\n    for fast != nil && fast.Next != nil {\n        slow = slow.Next\n        fast = fast.Next.Next\n    }\n    mid := slow.Next\n    slow.Next = nil // 断开\n\n    left := sortList(head)\n    right := sortList(mid)\n    return merge(left, right)\n}\n\nfunc merge(l1, l2 *ListNode) *ListNode {\n    dummy := &ListNode{}\n    curr := dummy\n    for l1 != nil && l2 != nil {\n        if l1.Val <= l2.Val {\n            curr.Next = l1\n            l1 = l1.Next\n        } else {\n            curr.Next = l2\n            l2 = l2.Next\n        }\n        curr = curr.Next\n    }\n    if l1 != nil {\n        curr.Next = l1\n    } else {\n        curr.Next = l2\n    }\n    return dummy.Next\n}",
      "timeComplexity": "O(n log n)",
      "spaceComplexity": "O(log n) 递归栈"
    }
  ]
}
