{
  "id": 53,
  "leetcodeId": 207,
  "title": "课程表",
  "category": "图论/拓扑排序",
  "difficulty": "Medium",
  "description": "给定课程总数和先修关系，判断是否可能完成所有课程（即判断有向图是否有环）。",
  "examples": [
    {
      "input": "numCourses = 2, prerequisites = [[1,0]]",
      "output": "true"
    },
    {
      "input": "numCourses = 2, prerequisites = [[1,0],[0,1]]",
      "output": "false",
      "explanation": "存在环，无法完成"
    }
  ],
  "hint": {
    "core": "拓扑排序检测有向图是否有环：入度为 0 的节点入队，逐步削减",
    "keyPoints": [
      "建邻接表 + 入度数组",
      "所有入度为 0 的课程入队",
      "BFS 弹出节点，将其后继入度减 1，新入度为 0 的入队",
      "最终处理的节点数等于课程总数则无环"
    ]
  },
  "solutions": [
    {
      "name": "BFS 拓扑排序",
      "code": "func canFinish(numCourses int, prerequisites [][]int) bool {\n    inDegree := make([]int, numCourses)\n    graph := make([][]int, numCourses)\n    for _, p := range prerequisites {\n        graph[p[1]] = append(graph[p[1]], p[0])\n        inDegree[p[0]]++\n    }\n    queue := []int{}\n    for i := 0; i < numCourses; i++ {\n        if inDegree[i] == 0 {\n            queue = append(queue, i)\n        }\n    }\n    count := 0\n    for len(queue) > 0 {\n        course := queue[0]\n        queue = queue[1:]\n        count++\n        for _, next := range graph[course] {\n            inDegree[next]--\n            if inDegree[next] == 0 {\n                queue = append(queue, next)\n            }\n        }\n    }\n    return count == numCourses\n}",
      "timeComplexity": "O(V+E)",
      "spaceComplexity": "O(V+E)"
    },
    {
      "name": "DFS 检测环",
      "code": "func canFinish(numCourses int, prerequisites [][]int) bool {\n    graph := make([][]int, numCourses)\n    for _, p := range prerequisites {\n        graph[p[1]] = append(graph[p[1]], p[0])\n    }\n    // 0=未访问 1=访问中 2=已完成\n    state := make([]int, numCourses)\n    var hasCycle func(int) bool\n    hasCycle = func(u int) bool {\n        state[u] = 1\n        for _, v := range graph[u] {\n            if state[v] == 1 {\n                return true // 环\n            }\n            if state[v] == 0 && hasCycle(v) {\n                return true\n            }\n        }\n        state[u] = 2\n        return false\n    }\n    for i := 0; i < numCourses; i++ {\n        if state[i] == 0 && hasCycle(i) {\n            return false\n        }\n    }\n    return true\n}",
      "timeComplexity": "O(V+E)",
      "spaceComplexity": "O(V+E)"
    }
  ]
}
